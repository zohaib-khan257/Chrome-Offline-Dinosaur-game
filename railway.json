{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn dino_game.wsgi --bind 0.0.0.0:$PORT"
  }
}
